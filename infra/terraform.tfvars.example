aws_region       = "us-east-1"
scanner_image_tag = "sha-REPLACE"
bedrock_model_id = "us.anthropic.claude-3-5-haiku-20241022-v1:0"

# Include only feasibility-approved canonical adapter endpoints.
source_endpoints = {
  # pinnacle = "https://approved-adapter.example/pinnacle"
}
